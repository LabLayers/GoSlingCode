extends layout

block content
  header#learn(style='background: url(img/contact.jpg);background-size:100%  100%;')
    h1.lead Contact Us!
    p.lead
      | For questions, help, or just for fun!

  form.form-horizontal.normal.contact-form(role='form', method='POST')
    input(type='hidden', name='_csrf', value=token)
    .form-group
      //label(class='col-sm-2 control-label', for='name') Name
      .col-sm-8
        input.form-control.col-sm-8(type='text', name='name',placeholder='Name', id='name', autofocus=true)
    .form-group
      //label(class='col-sm-2 control-label', for='email') Email
      .col-sm-8
        input.form-control.col-sm-8(type='text', name='email', placeholder='Email', id='email')
    .form-group
      //label(class='col-sm-2 control-label', for='message') Body
      .col-sm-8
        textarea.form-control.col-sm-8(type='text', name='message', placeholder='What do you want to say?', id='message', rows='7')
    .form-group
      .col-sm-offset-2.col-sm-8
        button.btn.btn-default(type='submit')
          i.fa.fa-mail-forward
          | Send

    script.
      $(document).ready(function(){
        $('.form-control, button').width($('header').width());
      })

